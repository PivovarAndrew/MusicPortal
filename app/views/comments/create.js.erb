if ("<%= @comment.parent_id %>" === "") {
   $("#comments").append('<%= j render @comment, locals: { comment: @comment } %>')
} else {
   $("#sub-comment-<%= @comment.parent_id %>").append('<%= j render @comment, locals: { comment: @comment } %>').show("slow")
}

$('.comment-input-area').val('')
