<%= form_with(model: user_album) do |form| %>
  <% if user_album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_album.errors.count, "error") %> prohibited this user_album from being saved:</h2>

      <ul>
        <% user_album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :new %>
    <%= form.text_field :new %>
  </div>

  <div class="field">
    <%= form.label :create %>
    <%= form.text_field :create %>
  </div>

  <div class="field">
    <%= form.label :index %>
    <%= form.text_field :index %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
