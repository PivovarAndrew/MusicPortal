<h1>Editing Track</h1>

<%= render 'form', track: @track %>

<%= link_to 'Show', @track, {class: "link-default"} %> 
   <div class="links-separator-default"> 
      |
   </div>
<%= link_to 'Back', tracks_path, {class: "link-default"} %>
